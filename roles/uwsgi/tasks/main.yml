- name: uwsgi configuration file
  template:
    src: pptools.ini.j2
    dest: /etc/uwsgi/apps-available/pptools.ini
    mode: 0644
    owner: root
    group: root
  notify: restart uwsgi

- name: uwsgi activation link for configuration file
  file:
    src: /etc/uwsgi/apps-available/pptools.ini
    dest: /etc/uwsgi/apps-enabled/pptools.ini
    owner: root
    group: root
    state: link

- name: Make sure uwsgi service is running
  systemd:
    state: started
    enabled: true
    name: uwsgi
